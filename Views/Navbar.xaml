<UserControl x:Class="BibliotekaWPF.Views.Navbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="720"
             Height="75"
             Background="Transparent">
   
    <Border BorderBrush="White" Opacity="0.8" BorderThickness="0,0,0,0.25" >
        <Grid HorizontalAlignment="Right" VerticalAlignment="Center" Width="500" Height="55">
            <Grid.ColumnDefinitions>
                <ColumnDefinition ></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button  Grid.Column="0" Name="NavbarButton1" IsEnabled="False" Visibility="Hidden" Click="Button1" Content="Admin Panel" Style="{StaticResource CustomButton}" HorizontalAlignment="Stretch"></Button>
            <Button Grid.Column="1" Name="NavbarButton2" Click="Button2" Content="Zaloguj się"  Style="{StaticResource CustomButton}" HorizontalAlignment="Stretch" >
            </Button>
            <Button Grid.Column="2" Name="NavbarButton3" Click="Button3" Content="Zarejestruj się" Style="{StaticResource CustomButton}" ></Button>
        </Grid>
    </Border>
  
</UserControl>
